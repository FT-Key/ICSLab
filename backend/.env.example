# Puerto del servidor
PORT=4002

# URL de MongoDB Atlas o local
MONGODB_URI=mongodb://localhost:27017/ingenieria_software

# Servir el build del frontend desde el backend en producción
# 1 = sirve estáticos (producción) - 0 = solo API (desarrollo)
SERVE_STATIC=0
